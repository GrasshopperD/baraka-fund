# Baraka Fund

Premium Islamic investment simulator — single-page React app with Monte Carlo projections.

## Run locally

```bash
npm install
npm run dev
```

Open the URL shown in the terminal (typically `http://localhost:5173`).

## Build

```bash
npm run build
```
